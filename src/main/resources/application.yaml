spring:
  main:
    allow-bean-definition-overriding: true

  application:
    name: project

  datasource:
    url: jdbc:postgresql://localhost:5432/quizera_db011
    username: postgres
    password: 1929
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080