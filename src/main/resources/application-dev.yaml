server:
  port: 8084

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/quizera_db011
    username: postgres
    password: 1929
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update